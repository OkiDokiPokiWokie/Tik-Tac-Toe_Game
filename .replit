modules = ["php-8.2", "nodejs-24"]

# Open this file in the editor by default
entrypoint = "public/index.php"

# Start the PHP server
run = "php -S 0.0.0.0:8080 -t public"

[[ports]]
localPort = 8080
externalPort = 80

[nix]
packages = ["psmisc"]

[agent]
expertMode = true
